# 🎨 Architecture Diagrams & Visual Guide

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         BROWSER                              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Sign In    │  │   Messages   │  │    Users     │     │
│  │     Page     │  │     Page     │  │     Page     │     │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘     │
│         │                  │                  │              │
│         └──────────────────┴──────────────────┘              │
│                            │                                 │
│         ┌──────────────────┴──────────────────┐             │
│         │                                      │             │
│    ┌────▼─────┐                         ┌─────▼────┐       │
│    │  Clerk   │                         │  Convex  │       │
│    │  Auth    │                         │  Client  │       │
│    └────┬─────┘                         └─────┬────┘       │
│         │                                      │             │
└─────────┼──────────────────────────────────────┼────────────┘
          │                                      │
          │ JWT Token                            │ WebSocket
          │                                      │
┌─────────▼──────────────────────────────────────▼────────────┐
│                      CLOUD SERVICES                          │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐         ┌──────────────────────────┐     │
│  │    Clerk     │         │        Convex            │     │
│  │   Service    │         │      (Backend)           │     │
│  │              │         │        
## 📚 Study These Flows

1. **Message Send Flow** - Most important for interviews
2. **Auth Flow** - Shows security understanding
3. **Presence System** - Shows real-time expertise
4. **Read Receipts** - Shows attention to UX details
5. **Database Relationships** - Shows schema design skills

---

Good luck with your interview! 🎉
                       │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 Key Takeaways

### Data Flow Pattern
```
User Action → Mutation → Database → Subscription → All Clients → Re-render
```

### Auth Pattern
```
Clerk JWT → Convex Validation → User Identity → Authorized Action
```

### Presence Pattern
```
Heartbeat (30s) → Update lastSeen → Compute Status → Display
```

### Real-time Pattern
```
WebSocket Connection → Data Change → Push Update → React Hook → UI Update
```

---
   │
│     ├─ Bulk mark as read                                │
│     ├─ Bulk delete messages                             │
│     └─ Batch unread count queries                       │
│                                                         │
│  6. Optimistic Updates                                  │
│     ├─ Message appears immediately                      │
│     ├─ Typing indicator instant                         │
│     └─ Read receipts instant                            │
│                                  boundaries                              │
│     └─ Dynamic imports                                  │
│                                                         │
│  4. Memoization                                         │
│     ├─ useMemo for computed values                      │
│     ├─ useCallback for event handlers                   │
│     └─ React.memo for expensive components              │
│                                                         │
│  5. Batch Operations                                 States)          │
│                                                         │
│  2. Debouncing                                          │
│     ├─ Typing indicator (300ms)                         │
│     ├─ Search input (300ms)                             │
│     └─ Scroll events (100ms)                            │
│                                                         │
│  3. Lazy Loading                                        │
│     ├─ Skeleton loaders (400ms delay)                   │
│     ├─ Suspense ───────────────────────────────────────────────────┐
│  OPTIMIZATION STRATEGIES                                │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. Database Indexes                                    │
│     ├─ by_clerk_id (users)                              │
│     ├─ by_conversation_id (conversations)               │
│     ├─ by_conversation_and_time (messages)              │
│     └─ by_conversation_and_user (typing                                             │
│  MessageList → displays messages                        │
│  ConversationList → displays conversations              │
│  UserList → displays users                              │
│  TypingIndicator → displays typing state                │
└─────────────────────────────────────────────────────────┘

NO Redux, NO Context API, NO Local State for server data
Everything flows from Convex → React Hooks → Components
```

---

## 🚀 Performance Optimizations

```
┌──────         │
│                                                         │
│  useMutation(api.messages.sendMessage)                  │
│  useMutation(api.messages.markMessageAsRead)            │
│  useMutation(api.typingStates.setTypingState)           │
└────────────────────┬────────────────────────────────────┘
                     │
                     │ Props
                     │
┌────────────────────▼────────────────────────────────────┐
│                  COMPONENTS                             │
│            ────────────────┘
                     │
                     │ WebSocket (Real-time)
                     │
┌────────────────────▼────────────────────────────────────┐
│                  REACT HOOKS                            │
│                                                         │
│  useQuery(api.messages.getMessages)                     │
│  useQuery(api.conversations.getUserConversations)       │
│  useQuery(api.users.getAllUsers)                        │
│  useQuery(api.typingStates.getTypingState)     ionList (Full screen)
   │  OR
   └─ Chat View (Full screen)
      └─ BottomNav
```

---

## 🔄 State Management Flow

```
┌─────────────────────────────────────────────────────────┐
│                    CONVEX (Source of Truth)             │
│                                                         │
│  ┌─────────┐  ┌──────────┐  ┌─────────┐  ┌─────────┐ │
│  │  Users  │  │Conversations│ │Messages │  │ Typing  │ │
│  └─────────┘  └──────────┘  └─────────┘  └─────────┘ │
└────────────────────┬────────────────────ionList (Sidebar)
│  │  ├─ ConversationListHeader
│  │  └─ ConversationItem (multiple)
│  │     ├─ Avatar
│  │     ├─ Name
│  │     ├─ LastMessage
│  │     ├─ Timestamp
│  │     └─ UnreadBadge
│  │
│  └─ Chat Area
│     ├─ ChatHeader
│     ├─ MessageList
│     │  └─ MessageBubble (multiple)
│     │     ├─ Avatar
│     │     ├─ Content
│     │     ├─ Timestamp
│     │     ├─ ReadReceipt
│     │     └─ Reactions
│     ├─ TypingIndicator
│     └─ MessageInput
│
└─ Mobile Layout (AnimatePresence)
   │
   ├─ Conversat                 All reference users.clerkId ───┘
```

---

## 🎨 Component Hierarchy

```
app/layout.tsx
│
├─ ClerkProvider
│  │
│  └─ ConvexProviderWithClerk
│     │
│     ├─ ThemeProvider
│     │  │
│     │  └─ HeartbeatProvider
│     │     │
│     │     └─ {children}
│     │
│     └─ Convex Real-time Connection (WebSocket)
│
│
app/(main)/messages/page.tsx
│
├─ Desktop Layout
│  │
│  ├─ Vertical Sidebar (Navigation)
│  │  ├─ Chats
│  │  ├─ People
│  │  ├─ ThemeToggle
│  │  └─ Profile
│  │
│  ├─ ConversatATES                      │  │
├──────────────────────────────────────────────────────┼──┤
│ _id (auto)                                           │  │
│ userId (references users._id)                        │  │
│ conversationId (indexed)                             │  │
│ lastTypingAt (indexed)                               │  │
└──────────────────────────────────────────────────────┼──┘
                                                       │
                                                       │
              │  │
│ sentAt (indexed)                                     │  │
│ isDeleted                                            │  │
│ readBy: [clerkId, clerkId] ──────────────────────────┼──┤
│ reactions: [{emoji, userId}]                         │  │
└──────────────────────────────────────────────────────┼──┘
                                                       │
                                                       │
┌──────────────────────────────────────────────────────┼──┐
│                   TYPING ST───────────────────────────────────┘
                │
                │ Referenced by
                │
┌───────────────▼─────────────────────────────────────────┐
│                      MESSAGES                           │
├─────────────────────────────────────────────────────────┤
│ _id (auto)                                              │
│ conversationId (indexed) ───────────────────────────────┤
│ senderId (clerkId) ──────────────────────────────────┐  │
│ content                                       ───────────┐
│                   CONVERSATIONS       │                 │
├───────────────────────────────────────┼─────────────────┤
│ _id (auto)                            │                 │
│ conversationId (indexed)              │                 │
│ participants: [clerkId, clerkId] ─────┘                 │
│ isGroup                                                 │
│ groupName                                               │
│ lastMessageAt                                           │
└───────────────┬──────                        │
│ clerkId (indexed) ←──────────────────┐                 │
│ name                                  │                 │
│ email                                 │                 │
│ lastSeen                              │                 │
└───────────────────────────────────────┼─────────────────┘
                                        │
                                        │ Referenced by
                                        │
┌───────────────────────────────────────┼───────┬─────┘
       │                          │
       └──────────┬───────────────┘
                  │
                  ▼
         ┌────────────────┐
         │  Navigate to   │
         │  /messages?    │
         │  conversationId│
         └────────────────┘
```

---

## 📊 Database Relationships

```
┌─────────────────────────────────────────────────────────┐
│                        USERS                            │
├─────────────────────────────────────────────────────────┤
│ _id (auto)                      │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  Query existing conversation     │
│  by conversationId               │
└──────┬───────────────────────────┘
       │
       ├─────────────┬──────────────┐
       │             │              │
   Exists?       Yes │          No  │
       │             │              │
       ▼             ▼              ▼
   Return      Return ID    Create new
   existing                 conversation
   ID                       └────```
User A clicks on User B in /users page
       │
       ▼
┌──────────────────────────────────┐
│  getOrCreateConversation()       │
│  mutation                        │
│                                  │
│  participantIds: [userA, userB]  │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  Generate deterministic ID       │
│                                  │
│  sortedIds = [userA, userB].sort()│
│  conversationId =                │
│    "userA_userB"                                   │ Real-time update
                  │
                  ▼
         ┌────────────────────┐
         │  User A's Browser  │
         │  (Message sender)  │
         └────────┬───────────┘
                  │
                  ▼
         ┌────────────────────┐
         │  Check readBy      │
         │  length > 1?       │
         │                    │
         │  Yes → Show ✓✓     │
         │  No  → Show ✓      │
         └────────────────────┘
```

---

## 🔄 Conversation Creation (1-on-1)

   ▼
         ┌────────────────────┐
         │  markMessageAsRead │
         │  mutation          │
         └────────┬───────────┘
                  │
                  ▼
         ┌────────────────────────────┐
         │  Convex Backend            │
         │                            │
         │  message.readBy = [        │
         │    "userA_id",             │
         │    "userB_id"  ← Added     │
         │  ]                         │
         └────────┬───────────────────┘
                  │

   immediately
```

---

## 📖 Read Receipt System

```
┌─────────────────────────────────────────────────┐
│  Message appears in User B's viewport           │
└─────────────────┬───────────────────────────────┘
                  │
                  │ IntersectionObserver
                  │ (50% visible)
                  │
                  ▼
         ┌────────────────────┐
         │  useMessageVisibility │
         │  hook detects        │
         └────────┬─────────────┘
                  │
               dicator.tsx             │
│                                  │
│  states.filter(s =>              │
│    s.userId !== me &&            │
│    now - s.lastTypingAt < 3000   │
│  )                               │
│                                  │
│  Display: "User A is typing..."  │
└──────────────────────────────────┘

When User A sends message:
       │
       ▼
┌──────────────────────┐
│  clearTypingState()  │
│  mutation            │
└──────┬───────────────┘
       │
       ▼
   Indicator
   disappears                              │
│    userId: currentUser,          │
│    conversationId: conv123,      │
│    lastTypingAt: Date.now()      │
│  }                               │
└──────┬───────────────────────────┘
       │
       │ Real-time update
       │
       ▼
┌──────────────────────────────────┐
│  User B's Browser                │
│  useQuery(getTypingState)        │
└──────┬───────────────────────────┘
       │
       │ Filter states
       │
       ▼
┌──────────────────────────────────┐
│  TypingIn     │
    │  else                           │
    │    return "Offline"             │
    └─────────────────────────────────┘
```

---

## ⌨️ Typing Indicator Flow

```
User A types in input
       │
       │ Debounced (300ms)
       │
       ▼
┌──────────────────────┐
│  setTypingState()    │
│  mutation            │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  Convex Backend                  │
│                                  │
│  INSERT/UPDATE typingStates      │
│  { │
         │  useQuery(users)   │
         └────────┬───────────┘
                  │
                  │ Compute status
                  │
                  ▼
    ┌─────────────────────────────────┐
    │  presenceStatus.ts              │
    │                                 │
    │  timeSince = now - lastSeen     │
    │                                 │
    │  if (timeSince < 10s)           │
    │    return "Active now" 🟢       │
    │  else if (timeSince < 5min)     │
    │    return "Recently active"         │  sendHeartbeat()   │
         │  mutation          │
         └────────┬───────────┘
                  │
                  ▼
         ┌────────────────────┐
         │  Convex Backend    │
         │                    │
         │  UPDATE users      │
         │  SET lastSeen =    │
         │      Date.now()    │
         └────────┬───────────┘
                  │
                  │ Real-time update
                  │
                  ▼
         ┌────────────────────┐
         │  All Clients       ity()
│  ✓ Validates JWT
│  ✓ Returns user info
│  ✓ Throws error if invalid
└────────────────────────────────────────────────────
```

---

## 👥 Presence System (Heartbeat)

```
┌─────────────────────────────────────────────────┐
│  HeartbeatProvider.tsx                          │
│  (Wraps entire app)                             │
└─────────────────┬───────────────────────────────┘
                  │
                  │ Every 30 seconds
                  │
                  ▼
         ┌────────────────────┐
  │
       │            │  users.syncUser()  │         │
       │            └────────────────────┘         │
       │                                            │
       │ 6. All subsequent requests                 │
       │    include JWT in headers                  │
       │                                            │
       ▼                                            │
┌──────────────────────────────────────────────────┘
│  Every Convex Query/Mutation
│  
│  const identity = await ctx.auth.getUserIdent─┐    ┌────────────────────┐         │
│   Browser    │    │  Next.js API       │         │
│   (Stored)   │    │  /api/webhooks/    │         │
└──────┬───────┘    │  clerk/route.ts    │         │
       │            └────────┬───────────┘         │
       │                     │                      │
       │                     │ 5. Sync user data    │
       │                     ▼                      │
       │            ┌────────────────────┐         │
       │            │  Convex Backend    │       ─────────┘
       │
       │ 2. Enters credentials
       │
       ▼
┌──────────────────────┐
│  Clerk Backend       │
│  - Validates         │
│  - Creates session   │
│  - Generates JWT     │
└──────┬───────────────┘
       │
       ├─────────────────────┬──────────────────────┐
       │                     │                      │
       │ 3. JWT Token        │ 4. Webhook           │
       │                     │    (user.created)    │
       ▼                     ▼                      │
┌─────────────  │   │ React    │
│ Re-render│      │ Re-render│   │ Re-render│
└──────────┘      └──────────┘   └──────────┘
       │                 │              │
       ▼                 ▼              ▼
   Message          Message         Message
   appears          appears         appears
```

---

## 🔐 Authentication Flow

```
┌─────────────┐
│   User      │
└──────┬──────┘
       │
       │ 1. Clicks "Sign In"
       │
       ▼
┌──────────────────────┐
│  Clerk Sign-In UI    │
│  /sign-in/[[...]]    │
└──────┬───────────┐
       │                 │              │
       ▼                 ▼              ▼
┌──────────┐      ┌──────────┐   ┌──────────┐
│ User A   │      │ User B   │   │ User C   │
│ Browser  │      │ Browser  │   │ Browser  │
└──────┬───┘      └──────┬───┘   └──────┬───┘
       │                 │              │
       │ 4. useQuery hook receives update
       │                 │              │
       ▼                 ▼              ▼
┌──────────┐      ┌──────────┐   ┌──────────┐
│ React    │      │ React             │
│  ✓ Validate JWT token                │
│  ✓ Check user is participant         │
│  ✓ Insert message to DB              │
│  ✓ Update conversation timestamp     │
└──────┬───────────────────────────────┘
       │
       │ 3. DB Updated
       │
       ▼
┌──────────────────────────────────────┐
│  Convex Real-time Engine             │
│  Detects data change                 │
│  Pushes update via WebSocket         │
└──────┬───────────────────────────────┘
       │
       ├─────────────────┬─────────)

```
┌─────────────┐
│  User A     │
│  Browser    │
└──────┬──────┘
       │
       │ 1. Types "Hello"
       │
       ▼
┌──────────────────────────────────────┐
│  MessageInputRedesigned.tsx          │
│  const sendMessage = useMutation()   │
└──────┬───────────────────────────────┘
       │
       │ 2. sendMessage({ content: "Hello" })
       │
       ▼
┌──────────────────────────────────────┐
│  Convex Backend                      │
│  convex/messages.ts                  │
│                                             │     │
│  │ - Auth       │ Webhook │ - Real-time DB           │     │
│  │ - User Mgmt  ├────────►│ - Queries/Mutations      │     │
│  │ - Sessions   │         │ - WebSocket Server       │     │
│  └──────────────┘         │ - Serverless Functions   │     │
│                           └──────────────────────────┘     │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 💬 Message Flow (Step-by-Step